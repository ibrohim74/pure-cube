import React from 'react';
import Nav from "./nav";

const Header = () => {
    return (
        <div>
            <Nav/>
            <header>
            </header>
        </div>
    );
};

export default Header;